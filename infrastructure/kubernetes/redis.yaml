apiVersion: apps/v1
kind: Deployment
metadata:
  name: automind-redis
  namespace: automind
  labels:
    app: automind
    component: redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: automind
      component: redis
  template:
    metadata:
      labels:
        app: automind
        component: redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
          name: redis
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          exec:
            command:
            - redis-cli
            - ping
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - redis-cli
            - ping
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: automind-redis
  namespace: automind
  labels:
    app: automind
    component: redis
spec:
  selector:
    app: automind
    component: redis
  ports:
  - port: 6379
    targetPort: 6379
    name: redis
  type: ClusterIP
